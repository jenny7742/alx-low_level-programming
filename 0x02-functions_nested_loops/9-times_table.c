#include <stdio.h>
#include "main.h"

/**
 * times_table - prints times table
 * Return: no return
 */
void times_table(void)
{
	int x, row, j;

	x = 0;
	while (x < 9)
	{
		row = 0;
		while (row < 9)
		{
			putchar(
		}
	
		putchar('\n');
		x++;
	}
}
