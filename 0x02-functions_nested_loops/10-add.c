#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers
 * @w: first integer
 * @x: second integer
 * Return: returns result
 */
int add(int w, int x)
{
	int result;

	result = w + x;

	return (result);
}
