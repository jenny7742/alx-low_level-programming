#include <stdio.h>
#include "main.h"

/**
 * print_most_numbers - prints from 0 to 9
 *
 * Return nothing
 */
void print_most_numbers(void)
{
	int number;

	for (number 48; number < 58; number++)
	{
		if ((c == 50) || (c == 52))
		{
			continue;
		}
	putchar(letter);
	}
	putchar(10);

}
