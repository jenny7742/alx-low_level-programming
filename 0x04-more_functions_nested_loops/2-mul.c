#include "main.h"

/**
 * mul - function for multiplication of two numbers
 * @a: first number
 * @b: second number
 * Return: returns result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);

}
